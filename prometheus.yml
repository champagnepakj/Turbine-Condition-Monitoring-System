global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'python-consumer'
    static_configs:
      - targets: ['turbine-consumer.default.svc.cluster.local:8000']
